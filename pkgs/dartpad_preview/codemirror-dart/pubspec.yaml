name: codemirror_dart
description: CodeMirror 6 support for Dart.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.12.0-0"

resolution: workspace

dependencies:
  codemirror_lang_dart: any
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
